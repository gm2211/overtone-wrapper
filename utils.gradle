ext.appendToFile = { file_path, line ->
    def file = new File(file_path)

    if (!file.exists())
        file.createNewFile()

    if (!file.text.contains(line)) 
        file.append("\n" + line)
}
