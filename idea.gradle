idea {
// The following 3 lines must stay commented out if this is used as a gradle
// sub-project
//    project {
//        languageLevel = '8'
//    }
    module {
        sourceDirs += file('src/main/scala')
        sourceDirs += file('src/main/java')

        testSourceDirs += file('src/test/scala')
        testSourceDirs += file('src/test/java')

        jdkName = '1.8'
    }
}
